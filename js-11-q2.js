// ? Question-2:completed this table(Y or N )

// | Keyword      | Global Scope | Block Scope | Function Scope | Can be re-assign | Can be re-declare |
// |--------------|--------------|-------------|----------------|------------------|-------------------|
// | var          |      y       |      y      |       n        |always and forever|always and forever |
// | let          |      y       |      n      |       n        |        y         |        n          |
// | const        |      y       |      n      |       n        |        n         |        n          |
 console.log("so i dont understand but\n we can access var every where if we define it in global scope but not function scope i mean if you define a variable in function scope with var you can not access it any where but it's own function.\n you can redeclare or reassign var where ever you want.const is ontochable and let is just touchable if you want to reassign it");